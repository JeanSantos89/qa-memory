# DECISIONS — log append-only (mini-ADRs)

> Append only. Never rewrite past entries. New decision → new entry at the bottom.
> Format: `## NNN — title` / Date / Context / Decision / Consequences.

## 001 — Projeto pessoal independente
- **Date:** 2026-05-30
- **Context:** Repo nasceu numa máquina corporativa cujos plugins sugerem um padrão de organização (pnpm/uv/Node22/Py3.13/docs-structure/epics). Mas o projeto é 100% pessoal do autor.
- **Decision:** Ignorar convenções de organização externas. Tratar como projeto independente. Escolher a melhor stack isoladamente.
- **Consequences:** Sem automações de organização, sem design system externo, sem regras de commit externas. Stack escolhida por mérito próprio.

## 002 — Stack do projeto
- **Date:** 2026-05-30
- **Context:** Spec original pede Node 18 + npm + Python 3.11 + requirements.txt. Não negociável p/ decisões de PRODUTO, mas tooling é livre num projeto pessoal.
- **Decision:** MCP server = TS strict + Node 20 LTS + pnpm + better-sqlite3 + @modelcontextprotocol/sdk + zod + Vitest. Ingestion = Python 3.11+ + uv + pyproject.toml + ruff + mypy + typer + pydantic v2 + pytest. Mantidas decisões de produto da spec (SQLite, two-pass, embeddings locais, caveman).
- **Consequences:** Tooling moderno; versões mínimas da spec respeitadas onde fazem sentido.

## 003 — Auto-healing via pre-commit hook que bloqueia
- **Date:** 2026-05-30
- **Context:** Risco de docs ficarem desatualizados → chats futuros desenvolvem com contexto errado (AI slop). Disciplina sozinha não garante.
- **Decision:** Desenvolvimento em blocos (1 bloco = unidade + testes + docs + 1 commit). Garantia mecânica: `.githooks/pre-commit` versionado via `core.hooksPath`. Bloqueia commit se código mudou sem doc vivo; bloqueia se schema mudou sem `docs/SCHEMA.md`. Docs vivos: CLAUDE.md, docs/STATE.md, docs/SCHEMA.md, docs/DECISIONS.md.
- **Consequences:** Cada commit carrega contexto atualizado. Bypass de emergência: `ALLOW_DOC_SKIP=1`. Conjunto de docs vivos pode evoluir conforme o projeto.

## 004 — Git isolado de conta de trabalho
- **Date:** 2026-05-30
- **Context:** Máquina logada em outra conta corporativa; repo é da conta pessoal JeanSantos89.
- **Decision:** Identidade git LOCAL (não global): user.name=JeanSantos89, email=jeansaantos89@gmail.com. Remote com username embutido (`JeanSantos89@github.com/...`) p/ GCM isolar credenciais. PAT fine-grained da conta pessoal.
- **Consequences:** Commits atribuídos à conta pessoal sem afetar config global da máquina.

## 005 — Toolchain instalado sem admin
- **Date:** 2026-05-30
- **Context:** Máquina sem pnpm/Python/uv. corepack falhou (EPERM em Program Files, sem admin).
- **Decision:** pnpm via `npm install -g` (user prefix `%APPDATA%\npm`). Python 3.13 via `winget --scope user`. uv via installer oficial (`~/.local/bin`).
- **Consequences:** Tudo em diretório de usuário, sem admin. PATH não persiste entre shells → comandos usam caminho completo ou prepend.

## 006 — Isolamento de dados sensíveis (repo vs instância)
- **Date:** 2026-05-30
- **Context:** Usuário quer usar conhecimento do produto do trabalho, mas não pode expor nada no repo (potencialmente público).
- **Decision:** Separação total: repo só código/docs/exemplos neutros. Runtime instance `.qa-memory/` (DB + config real + creds) inteiramente git-ignored. Tokens só via env var. Config versionada só como `config.example.yaml` neutro. Repo pode ser dogfood (qa-memory sobre si mesmo) p/ exemplos.
- **Consequences:** Nenhum dado de produto entra no git, mesmo se o repo virar público. Instância de trabalho roda local, isolada.

## 007 — Migration runner mínimo + schema espelhado inline
- **Date:** 2026-05-30
- **Context:** Bloco 1.2 precisa do schema SQLite nos DOIS packages (TS + Py). Sem ORM, sem lib de migration externa (CLAUDE.md: "no unnecessary abstractions", "no new dependency sem checar").
- **Decision:** Runner próprio em cada package: lista `MIGRATIONS[]` (version, name, sql) + fn `migrate(conn)` que cria `schema_migrations`, aplica pendentes em transação, idempotente. SQL embutido inline como string em cada linguagem (espelho intencional, exigido pelo CLAUDE.md). Conexão helper liga FK ON + WAL. `:memory:` p/ testes.
- **Consequences:** Zero deps novas. Mudança de schema = editar SQL nos 2 arquivos + `docs/SCHEMA.md` no mesmo commit (hook reforça). Espelho duplicado é custo aceito e explícito.

## 008 — better-sqlite3 ^12 (Node 24) + pnpm allowBuilds
- **Date:** 2026-05-30
- **Context:** Máquina roda Node 24.14.1. better-sqlite3 11.x não publica prebuilt p/ Node 24 → install tentava `node-gyp rebuild`, falhava (sem Python/VS build tools no PATH do gyp). pnpm 11.5 também bloqueia install scripts por padrão.
- **Decision:** Bump better-sqlite3 p/ `^12.2.0` (tem prebuilt Node 24). Habilitar build scripts via `allowBuilds: {better-sqlite3: true, esbuild: true}` em `pnpm-workspace.yaml`.
- **Consequences:** Install sem toolchain C++. Versão mínima da spec (Node 18) continua compatível em runtime; só o piso de dev subiu. Se baixar Node, prebuilt ainda cobre.

## 009 — Vertical slice MCP: query_behavior real (não mock) + CLI + seed dogfood
- **Date:** 2026-05-30
- **Context:** Bloco 2.2 fecha a Fase 2. Spec/STATE chamavam `query_behavior` de "mock". A camada repo do 2.1 já fazia busca real LIKE sobre SQLite — não há motivo p/ retornar dado falso.
- **Decision:** Tool `query_behavior` consome `repo.queryBehavior` (LIKE name+description, exclui deprecated, case-insensitive) — slice real ponta a ponta, não mock. McpServer via stdio (`StdioServerTransport`). CLI `qa-memory` com `status`/`list behaviors`/`seed`. Seed dogfood NEUTRO (qa-memory sobre si mesmo) p/ demo compartilhável. Regras/confidence ficam p/ bloco futuro (não entram nesse slice). `main()` guardado por `process.argv[1] === fileURLToPath(import.meta.url)` p/ não rodar em import (testes). `openDb` cria dir pai p/ DBs em arquivo.
- **Consequences:** Slice demonstrável: `qa-memory seed` → MCP `query_behavior` retorna dados reais. Sem mock a manter. Embeddings/ranking semântico chegam na Fase 3 (substituem o LIKE). Bin novo `qa-memory` em package.json (além de `qa-memory-mcp`).

## 010 — Auditoria de neutralidade p/ repo público
- **Date:** 2026-05-30
- **Context:** Usuário perguntou se pode tornar o repo público. CLAUDE.md proíbe chaves de projeto Jira reais. `docs/SCHEMA.md` usava uma chave de um projeto interno real como exemplo de `source_ref`.
- **Decision:** Auditar todo o histórico (`git log -p` por padrões sensíveis). Único achado: a chave de projeto interna → trocada por `PROJ-123` neutro. Não reescrever histórico (valor não é secreto, só viola neutralidade; custo de filter-repo não se justifica p/ chave de exemplo).
- **Consequences:** Repo apto a virar público após esse commit. Resto do histórico já limpo (creds/URLs/empresa/cliente ausentes; email pessoal no author é intencional).

## 011 — Wiring de ingestão: orquestrador puro + repo Py + CLI typer
- **Date:** 2026-05-30
- **Context:** Bloco 3.4 conecta as peças puras 3.1–3.3 (source→chunker→two-pass→embeddings) num pipeline persistido. Precisava de camada de persistência Py (não existia; só o lado TS tinha repo) e CLI (`qa_memory.cli:app` já referenciado no pyproject mas sem arquivo).
- **Decision:** `ingest_doc(conn, doc, extractor, embed_model)` em `pipeline/ingest.py` recebe deps por injeção (extractor + embed_model são Protocols já existentes) → testável com fakes, sem rede/torch/API key. Persistência em `db/repo.py` (funções livres espelhando o estilo de `repo/behaviors.ts`, sem ORM). Checksum-guard no início (`find_source_by_checksum`) → doc idêntico é skip total, zero LLM. Embeddings só de behaviors (entity_type='behavior', content=name+description) em batch único. Confidence de rules de PDF = 0.6 (meio da banda 0.5–0.8 do SCHEMA). CLI typer com `Annotated[...]` (não defaults com chamada → ruff B008). Persist numa transação (`conn.commit()` ao fim).
- **Consequences:** Fase 3 fechada — PDF ponta a ponta. Nenhuma dep nova. Embeddings de rules/incidents ficam p/ quando o ranking semântico substituir o LIKE (Fase futura). `query_behavior` (TS) ainda usa LIKE; os vetores gravados aqui ainda não são consultados — wiring de busca semântica é bloco próprio.

## 012 — query_risk: score derivado e transparente (Bloco 4.1)
- **Date:** 2026-05-30
- **Context:** Fase 4 abre com `query_risk`. Decisão de produto: como expressar risco? Opções — score sintético, sinais brutos, ou nível por criticality. Usuário escolheu score derivado.
- **Decision:** Score 0..1 puro em `risk.ts` (sem acesso a DB → testável isolado). Base = pior criticality entre behaviors casados (P0=1.0/P1=0.7/P2=0.4/P3=0.2/custom=0.5). Bônus de incerteza (+0.1 cada, capado em 1.0): behaviors não confirmados por QA / behaviors sem rules conhecidas / rules todas inferidas com confidence<0.7. Todo contribuinte ecoado em `reasons[]` — score nunca é caixa-preta. Level: ≥0.8 high / ≥0.5 medium / >0 low / sem match → unknown. Lado TS ganhou `repo/rules.ts` (não existia; só Python persistia rules) que esconde under_review (confidence<0.5, conforme SCHEMA L105). Incidents ainda NÃO entram no score (tabela vazia até ingestão Jira) — fica como addend futuro explícito.
- **Consequences:** Agente que consome o MCP vê score + porquê e decide profundidade de teste. Nenhuma dep nova, nenhuma mudança de schema. `update_rule`/override (escrita, qa_override=1) é o Bloco 4.2. Quando incidents existirem, somam ao score sem quebrar a interface.

## 013 — update_rule: resolução de behavior sem chute + override por id (Bloco 4.2)
- **Date:** 2026-05-30
- **Context:** Fase 4 fecha com a escrita em linguagem natural: QA afirma/sobrescreve uma rule. Duas operações distintas — criar rule nova num behavior, ou sobrescrever uma rule existente (ex.: promover uma inferência low-confidence a verdade).
- **Decision:** Tool único `update_rule`. Override → via `rule_id` explícito (`overrideRule` fixa confidence 1.0 + qa_override=1 + override_reason). Criação → resolve o behavior por texto livre com `queryBehavior` (mesmo LIKE), mas EXIGE match único: 0 → erro pedindo p/ criar behavior; >1 → devolve a lista e pede refinamento. NUNCA escolhe um behavior ambíguo sozinho (escrita é irreversível-ish; chute errado contamina conhecimento). `reason` é sempre obrigatório → trilha de auditoria (`override_reason`). `behaviors.confirmed_by_qa` NÃO é alterado: confirmar o behavior é um ato separado de definir uma rule (evita efeito colateral surpresa); fica como bloco futuro se fizer sentido.
- **Consequences:** Fase 4 fechada. Nenhuma dep nova, nenhuma mudança de schema. QA passa a alimentar conhecimento "da cabeça" por conversa (decisão 006/STATE). Resolução por LIKE herda a limitação atual (sem semântica) — quando a busca vetorial entrar, `update_rule` melhora de graça. Override por texto (sem id) ficou de fora p/ não reintroduzir ambiguidade na escrita.

## 014 — Modelo de ingestão agente-alimentado + superfície guiada (sem UI) + reordenação por risco
- **Date:** 2026-05-30
- **Context:** Pergunta de produto: como facilitar AO MÁXIMO o usuário adicionar fontes, token-friendly, "fazer o mínimo possível"? Discussão concluiu: qa-memory É um MCP server → o agente já é a interface. Usuário já tem Atlassian/Drive/Slack conectados via claude.ai MCP. Surgiu a tentação de construir uma UI/chat dedicado.
- **Decision:** (1) **Conectores = agente-alimentado, híbrido.** qa-memory NÃO implementa conectores próprios agora; expõe tools de ingestão burras (`add_to_memory`: text|path|url) e o AGENTE usa os MCPs que o usuário já tem p/ buscar conteúdo e alimentar. Conteúdo de path/url-público é lido/fetchado server-side (token-free); fontes com auth o agente busca e passa como texto. Conectores nativos + scheduler = futuro (subagent que cuida da memória sozinho). (2) **Inteligência mora no AGENTE, não em código de conector** — o "memory-keeper" automatizado será uma skill/subagent. (3) **Descoberta resolve-se com SUPERFÍCIE GUIADA (B), não UI:** MCP prompts (`/qa-memory:add`, `:setup`) + estado vazio que ensina + skill de onboarding. (4) **UI dedicada (C) ADIADA** — só se usuário não-técnico virar prioridade (usuário avisa). Público primário = "os dois/indefinido" → B serve ambos e é fundação de C, não desvio. (5) **Auto-init já está pronto** (openDb cria dir + migra no boot) → usuário não roda setup. (6) **Roadmap reordenado por risco→valor:** busca semântica (S.1) ANTES de add_to_memory, porque sem ela adicionar fontes só enche um buscador LIKE burro mais rápido — semântica prova a tese.
- **Consequences:** Zero conector pesado no curto prazo; entrega rápida e token-light. Custo: ingestão de fontes com auth depende do agente (gasta token do chat até o subagent automatizado existir). UI não fecha porta — plugaria nas mesmas tools/prompts. Próximo bloco = S.1 busca semântica (ver ADR 015).

## 015 — Busca semântica: query embedada via subprocess Python (não dep JS nativa)
- **Date:** 2026-05-30
- **Context:** Os vetores gravados na ingestão (float32 BLOB, 384-dim, all-MiniLM-L6-v2, só behaviors) nunca foram consultados — query_behavior/query_risk usam LIKE. Pra busca semântica é preciso embedar a QUERY em runtime com o MESMO modelo. O MCP é TS; o modelo vive no Python (sentence-transformers).
- **Options considered:** (A) `@huggingface/transformers` no TS — testado: puxa onnxruntime-node + sharp (deps NATIVAS, build scripts bloqueados pelo pnpm, mesmo gate do ADR 008) + footprint pesado (sharp é imagem, irrelevante p/ texto). (B) Subprocess Python que embeda a query reusando o LocalEmbeddingModel já instalado.
- **Decision:** Opção B. Python ganha um comando CLI `embed "<texto>"` que imprime o vetor como JSON (stdout limpo). O TS chama via subprocess (comando configurável por env `QA_MEMORY_EMBED_CMD`, default `uv run`), desserializa o BLOB float32 LE (espelha `pack_vector`/`unpack_vector`), e faz cosseno + ranking **no TS** (a lógica de retrieval mora junto das tools MCP; o Python só roda o modelo). `queryBehavior` vira HÍBRIDO: ranking semântico sobre behaviors que têm embedding + fallback LIKE p/ os que não têm (o seed dogfood, p.ex., não gera embedding — só a ingestão Python gera). `query_risk` herda de graça (chama queryBehavior por dentro). O `Embedder` é injetado (interface) → testes usam fake com vetores auto-consistentes, sem baixar torch/modelo.
- **Consequences:** Zero dep TS nova, vetores garantidamente no mesmo espaço (mesmo modelo+serialização), sem build nativo — honra CLAUDE.md. Custo: cold-start por query (spawn Python + load torch/modelo, ~segundos) e fragilidade de PATH no runtime (uv pode não estar no PATH do host) → comando configurável + fallback LIKE se o subprocess falhar. Otimização futura: helper Python "quente" (processo persistente) ou serviço de embedding. Validação ponta-a-ponta com modelo real fica p/ run manual (testes cobrem ranking com fake).

## 016 — add_to_memory: ingestão de texto via subprocess, texto por stdin (Bloco 5.1)
- **Date:** 2026-05-30
- **Context:** Concretiza o modelo agente-alimentado (ADR 014): a tool MCP que o agente chama p/ "lembrar" qualquer texto que ele já tem em mãos (página buscada por outro MCP, notas coladas, conhecimento manual). O pipeline de extração (LLM two-pass + embeddings) vive no Python e até agora só tinha entrada PDF.
- **Decision:** (1) Python ganha `TextSource` (source genérico, sem parsing/auth, checksum = sha256 do texto trimado, `source_type` default "conversation" mas sobrescrevível p/ taggear origem ex. confluence) + comando CLI `ingest-text` (reusa o MESMO `ingest_doc`). (2) TS expõe a tool `add_to_memory` que faz subprocess pro `ingest-text` (mesmo padrão/ADR 015 do embed — única fonte de verdade da extração no Python, sem duplicar two-pass em TS). (3) Texto vai por STDIN (`-`), não argv → foge de limite de tamanho/escaping. (4) `Ingester` é injetado (interface) → testes sem subprocess/API key. (5) ESCOPO do bloco = só TEXTO; roteamento de file-path (.pdf→PdfSource, .txt/.md→TextSource) e fetch de URL ficam p/ 5.2.
- **Consequences:** "Jogou, lembrou" funciona p/ texto: o agente busca o que quiser com as próprias tools e passa adiante — token-friendly (extração roda fora do chat). Custo: depende do runtime ter o pacote Python + ANTHROPIC_API_KEY; falha é reportada sem throw (tool devolve ok=false + msg). Cold-start por chamada (igual ADR 015). Validação ponta-a-ponta com LLM real = run manual (testes cobrem roteamento com fake ingester).

## 017 — LLM provider plugável: Anthropic | Gemini | Llama local (ollama), seleção por env
- **Date:** 2026-05-30
- **Context:** Validação do path LLM two-pass (ADR 016) ficou pendente por falta de `ANTHROPIC_API_KEY`. Usuário tentou Gemini mas a quota/key não é suficiente → vai rodar **Llama LOCAL** em outra máquina (sem custo de API). `LLMClient` já é Protocol (extractor nunca importa SDK vendor direto), mas a CLI hardcodava `AnthropicClient()` e o model id era fixo. Decisão de produto: providers plugáveis; suportar nuvem (Anthropic/Gemini) E local (Llama) — local é o caminho primário agora.
- **Decision:** (1) `GeminiClient` (wrapper do SDK unified `google-genai`, import lazy) implementando o Protocol `LLMClient`; JSON via `response_mime_type="application/json"`; tokens via `usage_metadata` (prompt_token_count/candidates_token_count). (2) `OllamaClient` p/ Llama local via API nativa `/api/chat` do daemon Ollama, usando **só stdlib `urllib`** (CLAUDE.md: no new dep sem necessidade — Ollama não exige SDK). Sem API key (daemon local). JSON forçado via `format="json"`; tokens via `prompt_eval_count`/`eval_count`. Base URL configurável (`QA_MEMORY_LLM_BASE_URL`, default `http://localhost:11434`). (3) Factory `make_llm_client()` escolhe por env `QA_MEMORY_LLM` = `anthropic` (DEFAULT, honra a spec) | `gemini` | `ollama` (alias `llama`); model override via `QA_MEMORY_LLM_MODEL`; provider desconhecido → ValueError. (4) CLI (`ingest` + `ingest-text`) usa `make_llm_client()` — seleção na borda, extractor agnóstico. (5) Dep nova só `google-genai>=1.0` (Gemini); Ollama = zero dep. Defaults: Gemini `gemini-2.5-flash`, Ollama `llama3.1`.
- **Consequences:** Path LLM rodável **localmente sem custo** (Llama via Ollama) — usuário clona em outra máquina, sobe `ollama serve` + `ollama pull llama3.1`, exporta `QA_MEMORY_LLM=ollama` e o pipeline funciona igual. Nuvem (Anthropic default / Gemini opt-in) continua disponível. Custo: +1 dep (`google-genai`); Ollama assume daemon local no ar (falha de conexão sobe como erro do subprocess → `add_to_memory` reporta ok=false). CLAUDE.md "token strategy" agora por-provider (haiku/flash/llama3.1, o mais barato de cada). A tool MCP `add_to_memory` (TS subprocess) herda de graça via env `QA_MEMORY_LLM` no runtime do subprocess. Validação ao vivo do path LLM = run manual com Ollama na outra máquina (testes cobrem factory + mapeamento de tokens dos 3 clients com fakes, sem rede/key/daemon).

## 018 — Superfície guiada (Bloco B): MCP prompts + estado vazio que ensina
- **Date:** 2026-05-30
- **Context:** Roadmap reordenado (ADR 014) marca "B — superfície guiada" como o passo p/ resolver descoberta SEM UI: usuário (técnico ou não) precisa saber o que qa-memory é e qual o primeiro movimento. Tools existiam (query/update/add) mas nada onboardava; estado vazio só dizia "No behaviors match", o que ensina nada.
- **Decision:** (1) Novo `prompts.ts` registra 2 MCP prompts via `registerPrompt`: `getting_started` (explica o que é + lista as 4 tools + ramifica em vazio-vs-semeado lendo `countBehaviors`) e `assess_change` (arg `area` → guia o fluxo query_risk→ler reasons→corrigir rule→focar teste). (2) `emptyStateHint()` é fonte única do texto de estado-vazio ("feed-then-query": use add_to_memory/update_rule, depois query). (3) `query_behavior` e `query_risk` chamam `emptyStateHint()` quando 0 resultados E DB vazio (`countBehaviors(db)===0`) — ensinam em vez de só "no match"; com DB não-vazio mantêm a msg curta "no match". (4) Prompts injetam o mesmo `db` do server (sem estado novo).
- **Consequences:** Descoberta funciona sem UI — o client Claude expõe os prompts no menu e o primeiro query num DB vazio orienta. Serve técnico e não-técnico (regra da spec). Custo: zero dep nova, zero schema. Os prompts são estáticos/derivados do count — não chamam LLM (token-light). Próximos passos do roadmap: install script (furo #2) + otimizar cold-start embedding (~10s/query). Validação ao vivo do path LLM two-pass FECHADA nesta máquina (ver STATE): `QA_MEMORY_LLM=ollama` + llama3.1 → `ingest-text` retornou "2 behaviors, 2 rules, 2 embeddings, 330 tokens".

## 019 — Install script (furo #2): setup Dia-0 em um comando + fix do dir da instância
- **Date:** 2026-05-30
- **Context:** Sem script, o "Dia 0 ~2min" era mentira: clonar exigia 5 passos manuais (deps TS, build, deps Py, criar instância, descobrir o comando MCP). Pior: o Python `connect()` NÃO criava o dir pai (o TS `openDb` criava) → num clone fresco `.qa-memory/` não existe e `ingest-text`/`add_to_memory` quebravam com "unable to open database file" (peguei isso na validação Ollito).
- **Decision:** (1) **Fix real do furo:** `db/__init__.py.connect()` agora cria `Path(path).parent` (parents=True, exist_ok=True) p/ file paths — espelha o `openDb` TS. (2) `scripts/install.ps1` (Windows, primário) + `scripts/install.sh` (POSIX, repo neutro) — mesma sequência: checa prereqs (node≥20, pnpm, uv; ollama opcional) → `pnpm install` + `pnpm build` → `uv sync` → inicializa instância (`node dist/cli.js seed`, ou `status` com `--no-seed`) → imprime o snippet de config MCP pra colar no Claude Code. Idempotente. Flags: `-Check`/`--check` (só prereqs), `-NoSeed`/`--no-seed`. (3) Gating em `$LASTEXITCODE`/artefato (não `$?`) e `$ErrorActionPreference=Continue` — PS 5.1 trata stderr de pnpm/tsc/uv como NativeCommandError terminating. Path do JSON escapado `\`→`\\`.
- **Consequences:** Clone→rodando num comando (`scripts/install.ps1`). Furo #2 fechado. Custo: scripts assumem prereqs instaláveis pelo usuário (não auto-instalam node/pnpm/uv — só apontam o how-to). +1 teste pytest (mkdir do parent). Validado de verdade nesta máquina: script rodou ponta-a-ponta, semeou 3 behaviors, emitiu snippet MCP válido. Próximo: otimizar cold-start embedding (~10s/query) ou 5.2 (file-path/PDF routing).

## 020 — Cold-start embedding: daemon `embed-serve` + `PersistentEmbedder` async (~20s → ~10ms/query)
- **Date:** 2026-05-30
- **Context:** Cada `query_behavior`/`query_risk` chamava `PythonEmbedder.embed` → `spawnSync("uv run qa-memory embed")`, processo Python frio TODA query. Medido: **~20s/query**. Breakdown: import sentence_transformers ~5.1s + load all-MiniLM ~4.3s + **encode só ~0.02s**. Custo 100% startup/reload do modelo, repetido a cada query. Inviabilizava usar de verdade (ninguém espera 20s/pergunta). Solução: manter o modelo carregado num processo quente.
- **Decision:** (1) **Python:** `pipeline/embed_serve.py` — `serve(model, stdin, stdout)` loop line-delimited JSON: req `{"text":...}`/`{"texts":[...]}` → resp `{"ok":true,"vectors":[...]}` (1 linha/req; linha em branco/EOF encerra). Tolera BOM UTF-8. Modelo carregado 1x; `main()` faz warmup + marca "ready" no stderr (stdout = só resposta). CLI `embed-serve`. (2) **TS:** `Embedder.embed` agora é **async** (`Promise<number[]|null>`) — propagou por `searchBehaviors` e pelos handlers `query_behavior`/`query_risk` (MCP SDK aceita handler async). `PersistentEmbedder` mantém um `embed-serve` quente: lê o stdout **event-based** (`stdout.on('data')` + busca de `\n`), NÃO `readSync` (a 1ª tentativa síncrona via `readSync(fd)` competia com o flowing-mode do stream Node → EOF aparente → fallback cold a cada query; abandonada). Requests **serializados** numa fila (`this.queue`) p/ resposta-por-linha não interleavar. Fallback p/ `PythonEmbedder` cold só se o server não sobe/morre. Cleanup: `index.ts` fecha o subprocess no `exit`/SIGINT/SIGTERM. (3) cwd: `uv run` precisa do dir do pacote ingestion → env `QA_MEMORY_INGESTION_DIR` (ou `QA_MEMORY_EMBED_CMD`); `embedCommand`→`serveCommand` troca o subcomando final `embed`→`embed-serve`.
- **Consequences:** **Medido nesta máquina: 1ª query ~10s (warmup), demais ~10ms** — 3 ordens de grandeza. query_behavior/query_risk agora usáveis ao vivo. Testes: 7 pytest (embed_serve, fake model) + 52 pytest total ✓; 49 Vitest ✓ (fakes viraram async); ruff/mypy/typecheck ✓. Custo: o MCP server mantém 1 processo Python residente (RAM do modelo) — aceitável, é o preço de ser quente; morre junto com o server. Achado de design: ler stdout de child de longa duração em Node TEM que ser event-based — `readSync` em pipe é frágil (Windows: EAGAIN/EOF errático).

## 021 — analyze_impact: análise de impacto via LLM (salto de memória pesquisável → copiloto de QA)
- **Date:** 2026-05-30
- **Context:** Rodar a 1ª user story ao vivo expôs o gap de produto (STATE): ao mudar/testar uma regra, o QA quer que o sistema REBATA — o que pode quebrar, o que tomar atenção, e se a mudança CONFLITA com regras já guardadas. `query_risk` só faz busca+score derivado; nunca raciocina sobre conflito. É a feature de maior valor que faltava.
- **Naming (cuidado registrado no STATE):** já existe o PROMPT MCP `assess_change` (prompts.ts, Bloco B) — guia estático que orienta o fluxo query_risk. Para não colidir nem quebrar esse prompt e seus testes, a TOOL nova chama-se **`analyze_impact`** (verbo distinto). Prompt-guia `assess_change` fica como está. Comando CLI Python = `assess` (curto, sem colidir com nada).
- **Decision:** (1) **Análise mora no Python** (onde o LLM vive — fonte única, igual extractor/ingest). `pipeline/impact.py`: `retrieve_related()` recupera behaviors+rules relacionados à mudança (HÍBRIDO espelhando search.ts: cosseno sobre embeddings de behaviors acima do floor 0.25 + backfill LIKE; esconde rules under_review confidence<0.5) → `analyze_impact()` monta prompt terse JSON, chama o `LLMClient` injetado, parseia `{"breaks":[...],"watch":[...],"conflicts":[{"rule","why"}]}` reusando o `_parse_json` do extractor; loga tokens via `TokenUsage` (CLAUDE.md). Prompt manda "responder no idioma da mudança" (i18n grátis no conteúdo, como na extração). (2) **CLI `assess "<mudança>"`** (stdin `-` p/ texto grande, igual ingest-text/embed) imprime SÓ JSON no stdout (ruído→stderr) p/ o subprocess parsear. (3) **TS** `assessor.ts` (`Assessor` interface + `PythonAssessor` via subprocess) espelha `ingester.ts`: env `QA_MEMORY_ASSESS_CMD` (override) + cwd via `QA_MEMORY_INGESTION_DIR`, texto por stdin, timeout 300s; parse falho/erro → `ok:false`+message (sem throw). Tool MCP `analyze_impact` (server.ts) injeta o assessor (default `PythonAssessor`) → `createServer(db, embedder?, ingester?, assessor?)`; renderiza MAY BREAK / WATCH WHEN TESTING / CONFLICTS + nota de rules-consultadas/tokens. (4) **Testes:** 5 pytest (retrieval semântico, hide under_review, parse+tokens, memória vazia, JSON malformado — fakes p/ LLM e embedder 384-dim, sem rede/torch/key) + 4 Vitest (listada, roteia+renderiza, falha sem throw, change vazio — fake Assessor). (5) **Sem mudança de schema** — só LÊ behaviors/rules/embeddings.
- **Consequences:** É o salto de produto: "memória pesquisável" → "copiloto de análise de impacto". O agente passa uma mudança em linguagem natural e recebe quebra/atenção/conflito ancorados nas regras REAIS da memória — não um score abstrato. Nenhuma dep nova, nenhum schema novo, honra CLAUDE.md (subprocess único-fonte, tokens logados, prompt terse). Custo: depende do runtime ter o pacote Python + provider LLM (QA_MEMORY_LLM; Ollama local = path primário) — falha reportada sem throw. Cold-start por chamada (spawn `uv run` frio, igual ingest — o daemon quente é só do embed). Validação ao vivo com Ollama = run manual (testes cobrem retrieval+parse+roteamento com fakes). 57 pytest ✓ / 53 Vitest ✓ / ruff/mypy/typecheck ✓. Otimização futura possível: reusar o embedder quente na retrieval (hoje `assess` carrega o modelo frio) e somar incidents ao raciocínio quando a tabela existir.

## 022 — incidents no risk score: "o que já quebrou aqui" entra no número (Bloco 6)
- **Date:** 2026-05-31
- **Context:** A tabela `incidents` existe no schema desde a Fase 1 mas estava ociosa (só migrations). O ADR 012 deixou o gancho explícito: "quando incidents existirem, somam ao score sem quebrar a interface". O histórico de falhas é o sinal mais forte que um QA tem — "o que já quebrou aqui" — e estava invisível no risco. Bloco de alto valor / baixo custo: schema já cobre, sem migration.
- **Decision:** (1) **`repo/incidents.ts`** (espelha rules.ts): `Incident`, `insertIncident` (default `occurred_at`/`created_at` = now), `listIncidentsForBehaviors` (mais recente primeiro; SEM gate de confidence — incident é fato, não inferência). (2) **`risk.ts` ganha addend de incidents** (3º ingrediente, sobre o bônus de incerteza): peso por severidade (P0=0.3/P1=0.2/P2=0.1/P3=0.05, custom/null=0.1) × recência (decay binário: incident fora da janela de 90 dias pesa metade — data ruim/ausente trata como recente, não desconta calado). Soma capada em **0.3** (a criticidade-base ainda domina). Todo incident que mexe no número aparece em `reasons[]` (incl. flag "(capped)") — score continua transparente, regra do STATE/ADR 012. `computeRisk` recebe `incidents=[]` + `now` (injetado → puro/testável). (3) **Tool MCP `record_incident`** (espelha update_rule): resolve behavior por texto único, NUNCA chuta (0 ou N matches → pede refino); grava title + severity?/description?/source_ref?/occurred_at?, `source_type="manual"`. (4) **`query_risk`** lista incidents do behavior (`⚠ broke: <title> [sev] (ref)`) e os inclui no `structuredContent`. (5) **Testes:** 4 Vitest risk (recente lifta+ecoa, decay metade, cap, sem-incident sem-ruído) + 4 Vitest server (listada, grava no único match, recusa sem match, incident lifta score e aparece no query_risk). (6) **Sem mudança de schema** (já cobre) — sem dep nova.
- **Consequences:** Fecha o loop história→risco do ADR 012: "o que já quebrou aqui" agora levanta o piso do risco de forma transparente e capada. Lado Python (extração/ingestão de incidents via Jira/CI) fica para quando o roteamento de fonte existir (Bloco 8) — hoje o agente grava via `record_incident`. Embeddings de incidents continuam fora (só behaviors são embedados). 61 Vitest ✓ / typecheck ✓ (Python intacto — não tocado neste bloco).

## 023 — areas: path → behaviors via glob nativo (Bloco 7)
- **Date:** 2026-05-31
- **Context:** A tabela `areas` (file_pattern glob ↔ behavior_ids) existia desde a Fase 1 e estava ociosa. É o elo do fluxo natural de QA: "vou mexer em `checkout/**/*.ts` → quais behaviors isso toca → qual o risco". Hoje o QA tinha que adivinhar o TEXTO da área; com areas ele passa o CAMINHO do arquivo. Decisão em aberto (STATE): glob em TS via dep nova (minimatch) vs LIKE vs nativo.
- **Decision (dep):** **glob nativo, ZERO dep.** Recon confirmou: `minimatch` NÃO está na árvore do mcp-server (nem transitiva; deps diretas = só sdk/better-sqlite3/zod) e nenhuma lib de glob no lockfile. CLAUDE.md proíbe dep nova sem necessidade → `globToRegExp` em `repo/areas.ts` cobre o que file_pattern precisa: `*` (dentro do segmento, vira `[^/]*`), `**` (cruza segmentos, vira `.*`), `?` (`[^/]`), resto literal escapado, anchored (`^...$`), separadores normalizados p/ `/` (path Windows casa com pattern POSIX). LIKE foi descartado (impreciso p/ glob).
- **Decision (wiring):** (1) `repo/areas.ts`: `Area`, `insertArea`, `listAreas`, `matchesGlob`/`globToRegExp` (puros), `behaviorIdsForPath` (união dedup dos behavior_ids de toda area cujo glob casa). (2) `repo/behaviors.ts` ganha `behaviorsByIds` (preserva ordem, pula deprecated/desconhecido). (3) Tool MCP `map_area` (file_pattern + behaviors[] resolvidos por texto único, NUNCA chuta — espelha update_rule/record_incident). (4) `query_risk`: helper `looksLikePath` (tem `/`/`\` ou extensão `.ext`) → se path, resolve via `behaviorIdsForPath` ANTES do semântico; sem match de area OU não-path → fallback `searchBehaviors`. Header e structuredContent expõem `resolvedVia: "area"|"search"` (transparência). (5) Testes: 7 Vitest areas (glob `*`/`**`/`?`/anchored/metachar literal + união/dedup/empty/listAreas) + 4 Vitest server (map_area listada, mapeia+resolve path, recusa sem match, path sem area → fallback). (6) SEM schema novo, SEM dep.
- **Consequences:** O QA passa o CAMINHO que vai tocar e o risco resolve sozinho pela área mapeada — fecha o elo arquivo↔behavior. `analyze_impact` aceitar path fica adiado (não bloqueia o valor do Bloco 7; reabrir junto do Bloco 8/fonte). Glob nativo é suficiente p/ file_pattern; se surgir caso exótico (braces `{a,b}`, negação) reabrir p/ minimatch com justificativa. 72 Vitest ✓ / typecheck ✓ (Python intacto).

## 024 — roteamento de fonte: file-path + URL (o 5.2 adiado) (Bloco 8)
- **Date:** 2026-05-31
- **Context:** `add_to_memory` só aceitava texto cru. O modelo agente-alimentado (ADR 014) fica mais natural se aceitar um CAMINHO local (.pdf→PdfSource, resto→texto) e uma URL pública (fetch server-side, token-free). Auth continua sendo do agente (página privada → ele busca e passa texto).
- **Decision (dep):** **ZERO dep.** URL fetch + HTML→texto via stdlib (`urllib.request` + `html.parser.HTMLParser`), espelhando o uso de urllib no OllamaClient (CLAUDE.md). Nenhuma lib de scraping.
- **Decision (Python):** (1) `sources/url.py` `UrlSource` (type="url"): `urllib` fetch com User-Agent, `_html_to_text` (HTMLParser que dropa script/style/head/noscript/template e colapsa whitespace) só se content-type for html, senão texto cru; checksum sha256 dos bytes brutos. (2) `sources/router.py` `source_for_path`: extensão `.pdf`→PdfSource, resto→TextSource lendo o arquivo (`source_type="file"`, `errors="replace"`); `FileNotFoundError` se não existe (falha alto). (3) `__init__` exporta TextSource/UrlSource/source_for_path. (4) CLI: `ingest-file <path>` (rota por extensão) + `ingest-url <url>` (trata URLError/OSError→Exit 1, recusa texto vazio). Tail compartilhado `_ingest_and_report(doc, budget)` (dedup do pipeline chunk→two-pass→embed→persist) — `ingest`/`ingest-text` refatorados p/ usá-lo.
- **Decision (TS):** `ingester.ts` — interface `Ingester` ganha `ingestPath`/`ingestUrl`; `PythonIngester` agora tem base command (`uv run qa-memory`) + subcomando por chamada (helper `run(subcommand,args,input?)`), com compat: override `QA_MEMORY_INGEST_CMD` terminando em `ingest-text` tem o sufixo removido. Tool `add_to_memory` aceita `text?`/`path?`/`url?` + `label?`/`source_type?` (source_type só p/ texto), exige EXATAMENTE UMA fonte (0 ou >1 → recusa, sem chutar precedência). Testes: 4 router + 3 url (pytest; fetch de rede NÃO testado — só HTML→texto/label/type) + 5 Vitest (roteia path, roteia url, recusa >1, recusa 0, + o de texto atualizado com `kind`).
- **Consequences:** add_to_memory virou superfície de ingestão de 3 bocas (texto/arquivo/URL) sem dep nem auth embutida — fiel ao ADR 014. SCHEMA: `sources.type` ganha `file`+`url` (sem migration, coluna livre). Conectores nativos/sync seguem futuro (subagent). Validação ao vivo (fetch real / PDF real) = run manual; testes cobrem roteamento + parsing com fakes/tmp. 76 Vitest ✓ / 64 pytest ✓ / ruff/mypy/typecheck ✓.

## 025 — i18n da camada de apresentação (Bloco 9)
- **Date:** 2026-05-31
- **Context:** ACHADO/i18n registrado desde a 1ª user story: o CONTEÚDO extraído pelo LLM segue o idioma da entrada (PT in → PT out), mas a MOLDURA da apresentação era hardcoded em inglês (`Risk: HIGH`, `MAY BREAK`, `Highest criticality at stake`, labels, reasons). Recon do Bloco 9 inventariou ~80 strings em risk.ts/server.ts/prompts.ts/cli.ts.
- **Decision:** (1) Novo `i18n.ts`: interface `Labels` (funções tipadas p/ cada string de moldura) + tabelas `EN` e `PT_BR` + `normalizeLang` (pt/pt-br/pt_BR→pt-BR, resto→en) + `getLabels(env)` por `QA_MEMORY_LANG` (default en). ZERO dep, ZERO schema. (2) `risk.ts`: `computeRisk(..., labels=getLabels())` — TODAS as `reasons[]` (incl. no-coverage, criticidade, unconfirmed, no-rules, all-inferred, incidents) agora saem da `Labels`; puro/testável (labels injetável). (3) `server.ts`: `const L = getLabels()` no `createServer`; query_risk (header, resolvedVia, no-known-rules, ruleTag QA/inferida, broke/quebrou), analyze_impact (header, MAY BREAK/WATCH/CONFLICTS/none/reasonedOver) e query_behavior (no-match) usam `L`. (4) Testes: 4 i18n (normalizeLang + getLabels en/pt) + 2 risk (reasons em pt-BR + no-coverage localizado). Testes EN existentes do server seguem verdes (default en).
- **ESCOPO HONESTO (débito restante):** traduzida a MOLDURA dos caminhos de MAIOR uso (query_risk / analyze_impact / query_behavior + reasons). Os **prompts MCP** (`prompts.ts`: getting_started/assess_change) e a **CLI** (`cli.ts`: status/help/seed) seguem hardcoded em inglês — são superfícies de menor tráfego e ficam como débito explícito p/ uma 2ª passada de i18n se o pt-BR virar uso real. Idiomas além de pt-BR = adicionar um objeto `Labels` (as chaves são o contrato).
- **Consequences:** Com `QA_MEMORY_LANG=pt-BR` o QA brasileiro vê risco/impacto na própria língua, moldura + conteúdo. Sem dep, sem schema, sem mudar a lógica de score (só a renderização). As strings novas dos Blocos 6/7/8 entraram no `Labels` (broke→quebrou, resolved via area→resolvido via área). 82 Vitest ✓ / typecheck ✓ (Python intacto).

## 026 — perf do assess: reusar o embedder quente (Bloco 10)
- **Date:** 2026-05-31
- **Context:** O ADR 020 resolveu o cold-start do QUERY path (daemon `embed-serve` quente, ~10ms/query). Mas o `assess` (analyze_impact) ainda carregava o modelo de embedding FRIO a cada chamada (`impact.py:retrieve_related` → `embed_model.encode([q])`, ~9s + spawn). Recon do Bloco 10 deu 2 opções: (A) impact.py consome o daemon quente, (B) o MCP injeta o vetor já embedado e passa pro subprocess. Recon recomendou **B** (mais simples; não replica o protocolo do daemon no Python; mantém embedding na camada TS).
- **Decision (Opção B):** (1) **Python**: `retrieve_related` e `analyze_impact` ganham `precomputed_vector: list[float] | None`; `embed_model` vira `EmbeddingModel | None`. Se vetor vem → usa direto e PULA o `encode` (e o load do modelo). CLI `assess`: stdin agora aceita texto cru OU JSON `{"change", "vector"?}` — se começa com `{` tenta parsear (fallback p/ texto cru se não for JSON válido); com vetor, instancia `embed_model=None` (zero load do torch). (2) **TS**: `Assessor.assess(change, vector?)`; `PythonAssessor` envia `{change,vector}` JSON por stdin quando há vetor, senão texto cru (Python aceita ambos — retrocompatível). Tool `analyze_impact` (server.ts) virou ASYNC: `await embedder.embed(change)` (embedder QUENTE já injetado, ADR 020) → passa o vetor pro assessor. Embedder indisponível (null) → Python embeda frio como antes (degrada, não quebra). (3) Testes: +1 pytest (precomputed_vector usado, `encode` NUNCA chamado — `BoomEmbed` levanta se chamado; e com vetor o modelo é ignorado) + 1 Vitest (vetor quente repassado ao assessor). Demais testes do assess seguem verdes (caminho sem vetor intacto).
- **Consequences:** `assess` ponta-a-ponta via MCP deixa de pagar o cold-load do embedding (o vetor vem do daemon quente, ~10ms) — a penalidade de ~9s registrada no ADR 021 cai p/ o caminho MCP. CLI direto sem vetor continua funcionando (embeda frio). Nenhuma dep, nenhum schema. Medição antes/depois ao vivo = run manual (testes provam o roteamento + skip do encode). **FECHA o roadmap de infra (Blocos 6–10).** 83 Vitest ✓ / 65 pytest ✓ / ruff/mypy/typecheck ✓.

## 027 — retrieval cross-idioma PT<->EN: traduzir a query + guarda de LLM (Bloco 11)
- **Date:** 2026-05-31
- **Context:** ACHADO AO VIVO (STATE): `analyze_impact` com a mudança em PORTUGUÊS sobre regras gravadas em INGLÊS voltou `conflicts: (nenhum)` + 0 regras — ERRADO (existia `No combining with loyalty discount`). Causa: o embedder all-MiniLM-L6-v2 é EN-cêntrico → cosseno cross-idioma fica abaixo do floor 0.25 E o LIKE não casa palavras de idioma diferente → retrieval traz 0 → o LLM não tem o que confrontar. PROVADO que a mecânica está certa: a MESMA mudança EM INGLÊS retornou o conflito exato. Era limite da retrieval quando idioma da consulta ≠ idioma da memória, não regressão. Único furo de PRODUTO encontrado ao vivo.
- **Decision (mitigação):** Das 3 opções (embedder multilíngue / normalizar na ingestão / **traduzir a query**), escolhida **traduzir a query antes do retrieve** — ZERO reindex dos vetores já gravados, reversível, barata. Escopo **PT<->EN apenas** (decisão de produto — os únicos idiomas em uso).
- **Decision (robustez entre LLMs — pedido do usuário):** o usuário roda llama local; outros podem usar Anthropic/Gemini. A tradução NÃO pode ser cega: é **VALIDADA** (não-vazia, mudou de fato, não é recusa, idioma-alvo correto via `detect_lang`). Modelo fraco que devolve lixo/eco do input NÃO contamina a retrieval → **degrada** p/ a query original E emite uma `note` (no idioma da mudança) avisando que a busca cross-idioma ficou limitada e sugerindo `QA_MEMORY_LLM_MODEL` mais forte. Nunca quebra, nunca bloqueia o assess (self-adapta + transparente — escolha do usuário).
- **Decision (abordagem bilíngue):** não adivinha o idioma da memória. `retrieve_related` coleta candidatos da query ORIGINAL **e** da tradução (união dedup de semântico+LIKE de cada) → memória mista PT+EN é alcançada pelos dois lados. A tradução é embedada FRIA (`embed_model.encode`) quando há modelo; o `precomputed_vector` quente só cobre a query original. Sem dep, sem schema, sem reindex.
- **Impl:** (1) Novo `pipeline/crosslang.py`: `detect_lang` (heurística PT vs EN — chars acentuados + stopwords PT, sem dep/sem LLM), `translate_query` (LLMClient injetado → `_validate` → `(tradução|None, note|None)`, nunca levanta — qualquer erro de transporte degrada). Notas EN/PT embutidas. (2) `impact.py`: `retrieve_related` agora retorna `_Retrieval(related, note)`, recebe `client?`; helper `_collect_candidates` (por variante de query, pula ids já vistos). `analyze_impact` repassa seu próprio `client` p/ a tradução e propaga `note` no `ImpactAnalysis`. (3) CLI `assess`: JSON de saída ganha `note`. (4) TS `assessor.ts`: `ImpactAnalysis.note?`; `analyze_impact` (server.ts) renderiza a nota (label i18n `noteLabel` — `⚠ Note:`/`⚠ Nota:`) + no structuredContent.
- **Consequences:** Query PT alcança regras EN (e vice-versa) sem reindexar — fecha o furo de produto p/ usuários PT. Custo: +1 chamada LLM curta de tradução por assess (tokens NÃO somados ao usage da análise; o usuário aceitou). A qualidade depende do LLM dar conta da tradução — com modelo fraco a feature se auto-desativa e AVISA, em vez de mentir silenciosamente. Embedder monolíngue continua (trade-off aceito vs. reindex). Validação ao vivo (PT→EN real com Ollama) = run manual; testes cobrem detect_lang, validação/degrade, união de candidatos traduzidos e propagação da nota com fakes. 84 Vitest ✓ / 71 pytest ✓ / ruff/mypy/typecheck ✓.

## 028 — memory-keeper Bloco 1: superfície de revisão `review_memory` (fila de curadoria)
- **Date:** 2026-06-01
- **Context:** Início do subagent **memory-keeper** (ADR 014 — inteligência no AGENTE, não em conector). Responsabilidade alvo: promover regras inferred → QA-confirmed, dedup, curadoria. Recon mostrou o furo: a PROMOÇÃO já existe (`update_rule` com `rule_id` → `overrideRule` fixa confidence 1.0 + qa_override), mas NINGUÉM consegue VER os candidatos. `listRulesForBehaviors` só retorna confidence ≥0.5 E exige behaviorIds; rules `<0.5` (under_review) são invisíveis em TODO o MCP; não há surface global de "o que falta confirmar". Sem isso o agente não tem worklist. Decisão do usuário (go): tool-first testável; agente consome no Bloco 2; dedup fica p/ Bloco 2+; fila inclui inferred (qa_override=0, conf≥0.5) **+** under_review (<0.5); behaviors não confirmados ficam fora por ora.
- **Decision:** (1) **`repo/rules.ts` `listUnconfirmedRules(db)`** → todas as rules com `qa_override=0` de behaviors não-deprecated, com contexto do behavior (nome+criticality) e flag `under_review` (confidence < `UNDER_REVIEW_BELOW`). Ordem: confidence ASC (mais fraca primeiro = mais precisa de decisão humana), depois created_at ASC. É a ÚNICA leitura que expõe under_review (bypassa o gate 0.5 de propósito — p/ resgatar/descartar inferência fraca). (2) **Tool MCP `review_memory`** (read-only, arg opcional `limit` default 50): agrupa a fila por behavior preservando a ordem weakest-first, mostra cada rule com `rule_id`+confidence+flag UNDER REVIEW, e instrui o fluxo (confirmar com o usuário → `update_rule` com o `rule_id` p/ promover). Fila vazia → "Nothing awaiting confirmation"; DB vazio → `emptyStateHint()`. structuredContent = `{count,total,pending[]}`. (3) Sem schema, sem dep. Promoção continua sendo `update_rule` (não duplica a escrita).
- **Escopo honesto (débito):** o texto de moldura de `review_memory` é EN (não passa pela `i18n.ts`) — mesmo tratamento de débito dos prompts.ts/cli.ts (ADR 025), superfície de baixo tráfego consumida sobretudo pelo agente via structuredContent. i18n dela = 2ª passada se pt-BR virar uso real. O AGENTE memory-keeper em si (.claude/agents) é o Bloco 2. Dedup de rules/behaviors = Bloco 2+ (precisa similaridade). Confirm de behaviors (confirmed_by_qa, ADR 013) segue fora.
- **Testes:** +3 Vitest rules (fila inferred+under_review weakest-first com contexto/flag/exclui qa_override; exclui behavior deprecated; vazia quando tudo confirmado) +4 Vitest server (listada; surfacing com ids+flag+exclui confirmada; fila limpa; loop fechado: promover via update_rule remove da fila). **91 Vitest ✓ / typecheck ✓** (Python intacto — não tocado).
- **Consequences:** o memory-keeper ganha worklist: uma chamada (`review_memory`) devolve tudo que espera confirmação, incl. inferências fracas antes invisíveis, cada uma com o `rule_id` p/ promover. Fecha o elo "ninguém promove" do STATE/Futuro. O loop de curadoria (ver candidato → confirmar com usuário → `update_rule`) está provado em teste ponta-a-ponta. Próximo (Bloco 2): o agente memory-keeper que orquestra esse loop + dedup.

## 029 — memory-keeper Bloco 2: o AGENTE de curadoria (`.claude/agents/memory-keeper.md`)
- **Date:** 2026-06-01
- **Context:** Bloco 1 (ADR 028) entregou a worklist (`review_memory`). Falta a inteligência que ADR 014 coloca no AGENTE: um subagent que puxa a fila, raciocina sobre cada candidato e conduz a promoção inferred→QA-confirmed. É o 1º artefato em `.claude/` do repo (decisão de tooling antes adiada no STATE — o usuário priorizou o memory-keeper agora).
- **Decision:** Subagent Claude Code `.claude/agents/memory-keeper.md` (frontmatter `name`/`description`/`tools`). `tools` = só as do server `qa-memory` (`mcp__qa-memory__review_memory`/`update_rule`/`query_behavior`/`query_risk`) — superfície mínima, sem acesso a escrita fora da curadoria. **Regra que domina tudo: nunca escreve em silêncio.** O QA (usuário) é a autoridade; o agente LÊ a fila, TRIA em 3 baldes (promover / resgatar-reescrevendo / descartar-ou-perguntar) e PROPÕE — `update_rule` só é chamado com aprovação explícita do usuário na conversa (default = dry-run). `reason` obrigatório em toda promoção (trilha de auditoria). Dedup fora (Bloco 3); criar behaviors/ingestão fora (é do `add_to_memory`/usuário). Saída terse, agrupada por behavior, weakest/under_review/P0-P1 primeiro, recomendação na margem.
- **Por que subagent e não tool:** a inteligência de triagem (julgar se uma inferência é regra real, contradição ou ruído) é raciocínio, não código determinístico — mora no agente (ADR 014). O determinístico (listar/promover) já é tool (Bloco 1 + `update_rule`).
- **Testes:** N/A — é config de agente (prompt), não código testável; o hook de doc não dispara p/ `.claude/**` (fora do `code_re`). A mecânica que ele invoca já é coberta (review_memory/update_rule no Bloco 1). Validação ao vivo (rodar o agente contra uma instância com fila real) = manual, depende do MCP `qa-memory` conectado no Claude Code consumidor — PENDENTE.
- **Consequences:** o loop de curadoria fica acionável por linguagem natural ("revisar a memória"), com a salvaguarda de que promoção exige aval humano — protege a memória de falsa confiança. Próximo (Bloco 3): detecção de duplicatas (rules/behaviors) p/ o keeper sinalizar merges; precisa similaridade (lexical normalizada primeiro, zero dep).

## 030 — memory-keeper Bloco 3: detecção de duplicatas `find_duplicate_rules` (lexical, zero dep)
- **Date:** 2026-06-01
- **Context:** O keeper (Bloco 2) sabia promover mas era cego a duplicatas — a extração LLM repete regras (a mesma política dita 2x, ou a mesma regra cai em behaviors diferentes). Faltava o sinal de dedup. STATE marcava similaridade lexical normalizada (zero dep) como 1ª escolha vs embeddings.
- **Decision (similaridade):** **lexical, ZERO dep** (CLAUDE.md: sem dep sem necessidade). `normalizeRuleText` (lowercase, não-alfanumérico→espaço via `\p{L}\p{N}` unicode-aware → mantém acentos PT, colapsa/trim) + Jaccard de token-sets. Duas regras entram no mesmo cluster se texto normalizado idêntico OU overlap ≥ threshold (`DEFAULT_DUP_THRESHOLD = 0.7`). Embeddings de rules ficam p/ depois (hoje só behaviors têm vetor; e o lexical já pega a maioria das duplicatas de extração). Cross-idioma NÃO é alvo aqui (regras em idiomas diferentes não são "duplicatas" textuais — é outro problema, resolvido no retrieval do ADR 027).
- **Decision (impl):** (1) `repo/rules.ts`: `normalizeRuleText` (export, puro), `findDuplicateRules(db, threshold?)` → clusters via **union-find** sobre todas as rules de behaviors não-deprecated (inclui under_review e cross-behavior — duplicata pode estar em qualquer lugar); retorna só clusters ≥2, maior primeiro, cada item com `rule`+`behavior_name`. O(n²) nos pares — ok p/ o tamanho de uma memória de QA. (2) Tool MCP `find_duplicate_rules` (read-only, `threshold?`): renderiza clusters com `rule_id`+flag (QA/conf/UR)+behavior; **detecção pura — NUNCA faz merge/delete** (não existe op; o keeper propõe canônica, o usuário decide). (3) Agente `memory-keeper.md`: ganha a tool no frontmatter + passo 5 (checar duplicatas, recomendar wording canônico, sinalizar o resto p/ aposentadoria) + boundary "dedup é detecção, não merge".
- **GAP honesto (vira Bloco 4):** não há como APOSENTAR a regra perdedora de um cluster — `rules` não tem coluna `status` e não há delete. Hoje o keeper sinaliza e o usuário promove a canônica via `update_rule`, mas a duplicata redundante permanece. Resolver de verdade exige uma op de retirada (provável migration: `rules.status` espelhado TS+Py + `docs/SCHEMA.md`) — decisão de schema, proposta ao usuário antes de implementar.
- **Testes:** +6 Vitest rules (normalize; cluster exato+near; cross-behavior c/ nomes; sensibilidade ao threshold; sem-overlap; exclui deprecated) +3 Vitest server (listada; cluster com ids; memória limpa). **100 Vitest ✓ / typecheck ✓** (Python intacto).
- **Consequences:** o keeper enxerga duplicatas determinística e consistentemente em toda a memória (não só na fila), sem dep nem schema. Fecha a parte de DETECÇÃO do dedup do ADR 014. A parte de RESOLUÇÃO (aposentar a redundante) é o Bloco 4, pendente de decisão de schema.

## 031 — memory-keeper Bloco 4: aposentadoria de regra via `rules.status` (migration 002)
- **Date:** 2026-06-01
- **Context:** Bloco 3 detectava duplicatas mas não dava p/ AGIR — não havia como aposentar a regra perdedora (rules sem `status`, sem delete), então `find_duplicate_rules` re-sinalizava o mesmo cluster p/ sempre. Usuário decidiu (recomendação aceita): migration de schema, opção mais limpa vs. hack de baixar confidence (que mentiria — a regra reapareceria como under_review).
- **Decision (schema):** **migration `002 rules_status`** — `ALTER TABLE rules ADD COLUMN status TEXT NOT NULL DEFAULT 'active'` (active|superseded). Espelhada TS (`migrations.ts`) + Python (`migrations.py`) + `docs/SCHEMA.md` no MESMO commit (regra do CLAUDE.md / hook). `status` é fato de CURADORIA, ortogonal a `confidence` (força da inferência) e a `qa_override`. ALTER com DEFAULT constante = retrocompatível (DBs existentes ganham 'active' em todas as linhas).
- **Decision (leituras filtram active):** `listRulesForBehaviors`, `listUnconfirmedRules`, `findDuplicateRules` (TS) e `_rules_for` do `impact.py` (Py) agora exigem `status='active'` — regra aposentada some de risco, fila, dedup e análise de impacto. `Rule`/`RuleRow`/`hydrate` ganham `status`; `insertRule` não lista a coluna (DEFAULT cobre → toda regra nasce active).
- **Decision (op de retirada):** **tool nova `retire_rule(rule_id, reason)`** (não estendi `update_rule` — mantém contratos de cada tool simples, espelha record_incident/map_area). `repo/rules.ts` `retireRule` (espelha `overrideRule`): seta `status='superseded'` + grava a razão em `override_reason` (trilha de auditoria da última ação de QA — documentado no SCHEMA) + `updated_at`; id desconhecido → null. Tool exige `reason` (auditoria), recusa id inexistente, irreversível pelas tools → confirmar antes. Agente `memory-keeper.md`: ganha `retire_rule` no frontmatter + passo de dedup acionável (promover canônica via update_rule → aposentar redundantes via retire_rule, só com aval) + boundary "detect→propose→(approval)→retire, nunca sozinho".
- **Testes:** TS +1 migration (status default active) +4 rules (default active; retira+oculta+razão; null id desconhecido; duplicata aposentada sai do dedup) +4 server (listada; retira por id e some do dedup; recusa id; exige reason). Py +1 migration (status default) +1 impact (retrieve esconde superseded). **109 Vitest ✓ / 73 pytest ✓ / typecheck ✓ / ruff ✓ / mypy src ✓.**
- **Débito pré-existente (não deste bloco):** `mypy` em STRICT sobre `tests/` acusa `test_router.py` (Bloco 8) — `tmp_path` sem anotação (4 funcs). Fora do escopo do Bloco 4 (arquivo não tocado); registrado p/ cleanup à parte. `mypy src` limpo.
- **Consequences:** fecha o dedup ponta-a-ponta (ADR 030 → 031): detectar → propor → aprovar → aposentar, e a memória fica de fato limpa (cluster resolvido não volta). 1ª migration além da 001 — exercita o runner de migrations em DB real (incremental, idempotente). O memory-keeper agora tem o ciclo completo de curadoria: promover inferências, resgatar under_review, detectar e aposentar duplicatas — tudo com aval humano. Candidatos restantes (sem bloco): embeddings de rules, dedup de behaviors, validação ao vivo do agente.

## 032 — guarda de neutralidade no git (pre-commit + pre-push), denylist local
- **Date:** 2026-06-01
- **Context:** Projeto pessoal rodando em máquina/conta do trabalho; vai ser "treinado" com conhecimento do produto da empresa. A regra de neutralidade (CLAUDE.md / ADR 010) dependia só de disciplina — e uma re-auditoria achou uma chave de projeto Jira interna real que vazou no título do ADR 010 e foi pro `origin`. Disciplina sozinha falha; precisa de garantia mecânica (como o hook de docs do ADR 003). Usuário escolheu a opção (B): hook que bloqueia.
- **Problema de design:** uma denylist com os termos REAIS da empresa (nome, domínio, chave de projeto) não pode ser commitada — a própria lista vazaria. Solução: separar genérico (rastreado) de específico (local/git-ignored).
- **Decision:** (1) `.githooks/neutrality-scan.sh` — lê um diff no stdin, varre só as linhas ADICIONADAS e bloqueia (exit 1) em: chaves tipo Jira `[A-Z]{2,5}-[0-9]{2,}` (menos allowlist de placeholders neutros: PROJ/CONF/SHA/UTF/RFC/…); credenciais (prefixos de chave de API de provedores, AWS, GitHub e Slack, header de chave privada, e bearer tokens longos); e termos da denylist LOCAL. (2) **Denylist local `.githooks/neutrality.local`** (git-ignored via `*.local` + linha explícita no `.gitignore`) — o usuário põe ali os termos da empresa; nunca é commitada. `.githooks/neutrality.local.example` (rastreado, neutro) documenta o formato. (3) `pre-commit` chama o scanner sobre o staged (bloqueia ANTES de entrar no git — proteção primária). (4) `pre-push` chama sobre os commits sendo enviados (defesa em profundidade; não bloqueia se não der p/ computar o range — o pre-commit já guardou o que entrou). (5) `.githooks/` é EXCLUÍDO da varredura: os próprios scripts contêm literais de padrão de credencial que se auto-disparariam. Bypass raro: `ALLOW_NEUTRALITY_SKIP=1`.
- **Limitação honesta:** varre linhas adicionadas de diffs, não mensagens de commit nem o histórico já existente (a chave Jira removida segue no passado — repo privado, rewrite só se necessário). Allowlist/denylist são heurísticas — a `neutrality.local` é a defesa real contra os termos específicos. Hooks valem só nesta máquina (precisam `git config core.hooksPath .githooks`, já feito).
- **Testes:** validado à mão — diff com uma chave Jira interna + um prefixo de credencial + termo da denylist → BLOQUEIA (exit 1, aponta cada um); diff só com `PROJ-123`/`SHA-256` → PASSA (exit 0). `neutrality.local` confirmada git-ignored; `.example` rastreável. (Hook = shell, sem suíte automatizada — mesma natureza do pre-commit do ADR 003.)
- **Consequences:** o vazamento que aconteceu (chave Jira no doc) agora é barrado na origem, e o modo "treino" fica protegido: ainda que se descreva algo real num doc/seed/teste rastreado, o commit/push trava. Custo: heurística pode ter falso-positivo (bypass existe) e exige o usuário preencher `neutrality.local` com os termos da empresa p/ cobrir nome/domínio/produto. Não substitui a separação principal (dado real só em `.qa-memory/`, git-ignored) — é a rede de segurança.

## 033 — i18n 2ª passada + comando feed no Python CLI (débitos ADR 025 + pendência 2026-06-03)
- **Date:** 2026-06-04
- **Context:** (1) `prompts.ts` e `cli.ts` tinham strings EN hardcoded (títulos dos MCP prompts + mensagem de erro do feed) fora do módulo `i18n.ts` — débito registrado no ADR 025. (2) O comando `qa-memory feed` existia no TS CLI mas não no Python CLI (`uv run qa-memory`), que é o usado no dia a dia. Usuários sem ANTHROPIC_API_KEY configurada localmente precisam do caminho sem LLM.
- **Decision:** (1) Adicionar 5 labels ao contrato `Labels` + tabelas EN/PT_BR: `promptGettingStartedTitle/Desc`, `promptAssessChangeTitle/Desc`, `feedInvalidJson`. `prompts.ts` resolve via `getLabels()` no início de `registerPrompts`; `cli.ts` usa `getLabels().feedInvalidJson` no catch de parse. (2) Novo `@app.command() feed()` no `cli.py` Python: lê JSON do stdin (`{source?, behaviors: [{name, description, criticality, rules?: [...]}]}`), persiste direto no SQLite (sem LLM), embeda behaviors e rules com `LocalEmbeddingModel` local. Mesma semântica do `feedKnowledge` TS.
- **Consequences:** débito ADR 025 totalmente fechado. CLI Python agora tem paridade de funcionalidade com o TS para o caminho sem LLM. Nenhuma dependência nova; zero schema; zero migration. Testes: +2 Vitest (i18n) + 4 pytest (feed CLI).

