// qa-memory MCP server entry point.
// Block 1.1: placeholder only. Real stdio MCP wiring lands in Fase 2.
export const VERSION = "0.0.0";

function main(): void {
  // Intentionally empty until Fase 2 (MCP stdio transport + tools).
}

main();
